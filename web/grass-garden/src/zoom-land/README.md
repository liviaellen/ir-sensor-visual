Zoom-land

This folder contains a small close-up scene showing a flat, matte ground (no water, no glass) suitable for a zoomed-in view.

Files:

Usage:

Example:

import ZoomScene from "./zoom-land/ZoomScene";

function App() {
  return <ZoomScene />;
}
This folder has been deprecated — zoom-land removed from the app.
function App() {
